use banco_feito_de_dados;
CREATE TABLE funcionarios (
	codigo INTEGER NOT NULL AUTO_INCREMENT,
    nome VARCHAR(255) NOT NULL,
    PRIMARY KEY (codigo)
);

CREATE TABLE projetos (
	codigo INTEGER NOT NULL AUTO_INCREMENT,
    nome VARCHAR(255) NOT NULL,
    PRIMARY KEY (codigo)
);
